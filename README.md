Spring MVC project using Kotlin and Thymeleaf

This webapp displays the BPM (beats per minute) that someone is able to tap along with using two keys.

The display updates in real-time relatively accurately.
